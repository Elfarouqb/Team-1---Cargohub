name: CargoHub CI Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read
  security-events: write  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Debug directory structure
      - name: List Files
        run: ls -R
        working-directory: .

      # Set up .NET SDK for .NET 8.0
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      # Restore .NET dependencies
      - name: Restore dependencies
        run: dotnet restore
        working-directory: CargoHubV2/Cargohub V2/Cargohub V2

      # Initialize CodeQL (default auto-detect)
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3

      # Build the Solution (Required for CodeQL)
      - name: Build Solution
        run: dotnet build --configuration Release
        working-directory: CargoHubV2/Cargohub V2/Cargohub V2

      # Run CodeQL Analysis (default queries)
      - name: Run CodeQL Analysis
        uses: github/codeql-action/analyze@v3

